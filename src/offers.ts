import { OffersModel } from "./appModel";

// In real scenario this data come from database and admin can add there offer in this
export const Offers: OffersModel = {
    atv: { offer: "by2get1" },
    ipd: { offer: "bulk", discount: 50, minItem: 4 }
}
